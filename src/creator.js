// Compatibility wrapper.
// Project sekarang login-only, jadi batch executor lama diarahkan ke login batch.

export { loginAccountsBatch } from './login-batch.js';

export { loginAccountsBatch as createAccountsBatch } from './login-batch.js';
